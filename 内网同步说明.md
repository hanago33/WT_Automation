# 内网同步说明（便携 Python + 增量发布包）

适用于：**改代码在外网机（有网），验证程序在内网机（离线）** 的场景。
传输方式：通过文件传输软件（飞书/钉钉/微信/网盘等）发送文件，**不使用 U 盘、不直接传全量代码**。

---

## 方案总览

| 环节 | 方案 | 传输物 | 频率 |
|------|------|--------|------|
| **全新内网机器** | 全量包 + 便携 Python | `全量包_*.zip`（约 120MB）+ `wt_python_portable.zip`（约 210MB） | **只传一次** |
| **内网运行环境** | 便携 Python（推荐） | `wt_python_portable.zip`（约 210MB） | **只传一次** |
| **代码/资源更新** | 增量发布包 | `发布包_*.zip`（几十 KB） | **每次改动** |

> 为什么内网不装 Python、用便携目录？
> 本项目是「源码 + 脚本调度」结构（`WT_Launcher` 靠 `__file__` 定位资源、用 subprocess 调 `WT_AUT_recorded.py`/`WT_Flow_Editor.py` 等兄弟脚本），整体打包成单个 exe 会大量改代码、易翻车。便携 Python 把「解释器 + 全部依赖」装进一个文件夹，内网解压即用、直接跑源码，**项目代码一行不改**，增量更新仍是最轻量的几十 KB。

---

## 〇、全新内网机器首次部署（只做一次）

全新机器需传 **3 个文件**（都在外网机 D 盘）：

| 文件 | 大小 | 说明 |
|------|------|------|
| `D:\wt_python_portable.zip` | 212MB | 便携 Python 环境 |
| `release_out\全量包_*.zip` | 约 120MB | 完整项目源码 + 资产（外网机 `python make_release.py --full` 生成） |
| `deploy_release.exe` | 4.9MB | 之后日常增量更新用（仓库根目录） |

内网机步骤：
1. 解压 `wt_python_portable.zip` → `D:\wt_python`；
2. 解压 `全量包_*.zip` → `D:\WT_Automation`（包内即项目根结构，解压即用）；
3. 双击仓库里的 **`启动WT自动化总控台_内网.bat`** 启动（它直接用便携 Python 全路径，**不需要内网安装 Python**）；
4. 如果打不开，先双击 **`验证环境_内网.bat`** 排查（见「常见问题」）；
5. 之后日常更新走第二章的增量流程。

> **重要**：内网机不要装 Python，也不要直接双击 `.py` 文件——`.py` 依赖系统关联的 Python，内网没有就会打不开。启动一律用 `启动WT自动化总控台_内网.bat`（内部写死便携 Python 路径 `D:\wt_python\python.exe`，若便携包解压到别的路径，请修改 bat 顶部的 `PYTHON` 变量）。

---

## 一、内网运行环境：便携 Python（只做一次）

### 1. 外网机制作（已帮你做好，当前已存在）
- 便携目录：`D:\wt_python`（Python 3.8.10 + 项目全部依赖）
- 压缩包：`D:\wt_python_portable.zip`（212MB）

已装依赖：pywinauto / pyautogui / pynput / opencv-python / numpy / Pillow / requests / openpyxl / pywin32 / comtypes 等，已实测 `import` 与项目核心模块全部正常。

### 2. 内网机部署
1. 把 `wt_python_portable.zip` 通过软件发到内网（仅此一次）。
2. 解压到 `D:\wt_python`（解压后约 600MB，确认磁盘够）。
3. **不需要任何安装**，直接调用：
   ```bat
   D:\wt_python\python.exe D:\WT_Automation\WT_AUT_recorded.py
   ```
4. 启动总控台用随项目提供的 **`启动WT自动化总控台_内网.bat`**（双击即可，内部写死便携 Python 路径）；若便携包解压位置不是 `D:\wt_python`，改 bat 顶部的 `PYTHON` 变量。

> 若内网缺 VC++ 运行库导致个别包报错（极少数精简系统），把外网机 `C:\Windows\System32\vcruntime140.dll`、`msvcp140.dll` 一并拷到 `D:\wt_python\` 即可。

### 3. 外部运行时依赖（便携 Python 之外，按需部署）

以下组件**不在便携 Python 内**，属独立运行时，按内网实际需要的功能决定是否部署：

| 组件 | 位置 | 是否必需 | 说明 |
|------|------|---------|------|
| Tesseract OCR | 仓库内 `tools/ORC/` | 模板制作/OCR 命名要 | 随仓库增量同步即可，**无需安装**，代码会自动找到 |
| pywinauto Recorder | 外网机 `D:\Pywinauto Recorder\pywinauto_recorder` | 可选（录制功能） | 其 `player` 模块已包含在便携 Python 内；录制 GUI 需把整个目录拷贝到内网才能用 |
| UI-TARS Desktop | 外网机 `C:\Users\14830\UI-TARS-desktop` | 可选（AI 自动采集） | Node.js 项目 + 仓库依赖，路径在代码中硬编码，内网要用需整体部署 |
| Inspect / Accessibility Insights | 系统工具 | 可选（半自动采集辅助） | 需单独安装，非项目文件 |

> **Python 版本说明**：项目代码 Python 3.8 与 3.11 均可运行（文档推荐 3.11，但已在外网机 3.8 下长期运行验证，两版语法均通过）。便携 Python 当前为 **3.8.10**，与外网机运行版本一致，可直接使用；若团队统一到 3.11，需重新制作便携环境。

---

## 二、日常增量同步（每次改动）

### 外网机（改完代码）
1. 双击 `make_release.exe`（必须在仓库目录内，它靠 git 定位仓库）。
2. 在 `release_out/` 生成 `发布包_时间戳_短哈希.zip`，只含相对上次同步点的增量：
   - 已提交改动 + 未提交修改 + 新增未跟踪文件；
   - 自动排除临时文件、`dist/`、`.zcode/` 及同步工具自身，体积通常几十 KB。
3. 用传输软件发这个 zip 到内网机。

### 内网机（收到 zip）
1. 把 zip 与 `deploy_release.exe` 放同一目录。
2. 双击 `deploy_release.exe`：自动找到最新 `发布包_*.zip` → 解压 → 输入内网仓库根目录（如 `D:\WT_Automation`）→ 回车完成覆盖/删除。
3. 命令行用法：`deploy_release.exe 发布包_xxx.zip D:\WT_Automation`

---

## 三、工具清单（均在仓库根目录）

| 文件 | 机器 | 作用 |
|------|------|------|
| `make_release.exe` | 外网 | 生成增量发布包 |
| `deploy_release.exe` | 内网 | 解压 + 覆盖一键部署 |
| `apply_release.exe` | 内网 | 仅覆盖（备用，需手动解压） |
| `make_release.py` / `apply_release.py` / `deploy_release.py` | 两端 | 源文件，便于改逻辑或重打包 |
| `make_release.py --full` | 外网 | 生成「全量包」（全新内网机 / 换机器时用） |

---

## 四、同步基准说明

- `make_release.exe` 用本地 git 标签 `wt_last_sync` 记录「上次同步点」，每次生成增量后自动更新。
- 强制全量重发：外网机 `git tag -d wt_last_sync` 后重跑（只带当前工作区 + 未跟踪文件）。
- 全新内网机 / 换机器：用 `python make_release.py --full` 生成全量包（含全部正式文件），与便携包一起一次传到位。
- 外网机重置仓库时，请同步 `wt_last_sync` 标签或走一次强制全量。

---

## 五、常见问题

- **内网打不开 / 提示找不到 Python？** 不要在内网装 Python。先双击 **`验证环境_内网.bat`** 逐项排查：
  1. 第 1 步报 `[ERROR] Not found: D:\wt_python\python.exe` → 便携包没解压到位，请解压 `wt_python_portable.zip` 到 `D:\wt_python`；
  2. 第 2 步 `DEPS OK` 没出现 → 便携包损坏或解压不完整，重新解压；
  3. 第 3 步 `WT_Launcher.py found` 没出现 → 全量包解压不完整。
  排查通过后，用 **`启动WT自动化总控台_内网.bat`** 启动（而不是双击 `.py`）。若便携包解压到其它位置，改两个 bat 顶部的 `PYTHON` 变量。
- **exe 必须在仓库目录内运行**：`make_release.exe` 靠 `git rev-parse` 定位仓库。
- **中文乱码？** 发布包用 Python `zipfile` 解压（UTF-8），内网请用 `deploy_release.exe` 而非资源管理器手动解压。
- **资源/配置改了什么？** json/png 等属代码资产，改动会自动进增量包；exe 只打包代码逻辑——本方案资源本就外置，天然覆盖。
- **需要删除文件？** 删文件写入 `deleted.txt`，内网机自动删除。

---

## 六、便携环境更新（何时需要重做）

平时**不需要**重做便携环境——代码更新全走增量包，不碰 Python。
只有以下情况需要在外网机重新打包：
- 内网换新机器；
- 项目新增了第三方依赖（改 `requirements.txt` 后）：

```powershell
D:\wt_python\python.exe -m pip install -r requirements.txt
# 重新压缩：
python -c "import os,zipfile; src='D:/wt_python'; out='D:/wt_python_portable.zip'; z=zipfile.ZipFile(out,'w',zipfile.ZIP_DEFLATED,compresslevel=6); [z.write(os.path.join(r,f), os.path.relpath(os.path.join(r,f),src)) for r,ds,fs in os.walk(src) if '__pycache__' not in r for f in fs if not f.endswith('.pyc')]; z.close()"
```

---

## 七、重新打包同步工具 exe（如需改动脚本）

```powershell
cd 仓库根目录
python -m PyInstaller --onefile --console --name make_release   --noconfirm make_release.py
python -m PyInstaller --onefile --console --name deploy_release  --noconfirm deploy_release.py
python -m PyInstaller --onefile --console --name apply_release  --noconfirm apply_release.py
Copy-Item dist\make_release.exe   make_release.exe   -Force
Copy-Item dist\deploy_release.exe deploy_release.exe -Force
Copy-Item dist\apply_release.exe  apply_release.exe  -Force
Remove-Item build -Recurse -ErrorAction SilentlyContinue
```

---

## 八、日常速查

```
外网机（有网）                    内网机（离线）
─────────────                    ─────────────
改代码
双击 make_release.exe
  ↓ release_out/发布包_*.zip
── 传输软件发送 ────────────────→ 解压 zip 到与 deploy_release.exe 同目录
                                   双击 deploy_release.exe
                                     输入 D:\WT_Automation
                                     D:\wt_python\python.exe WT_Launcher.py
```
