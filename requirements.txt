# WT_Automation 统一依赖清单
# 安装：pip install -r requirements.txt
# 平台：Windows（桌面 RPA 自动化，依赖 pywinauto / 原生 Win32 API）

# ── 桌面自动化核心 ──
pywinauto>=0.6.8
pyautogui>=0.9.54
pynput>=1.7.6            # 半自动采集“交互点击”首选方案；缺失时自动降级到零依赖的 Windows 原生钩子（win32_input_hook.py）

# ── 控件库 / 图像模板构建 ──
opencv-python>=4.7.0
numpy>=1.23.0
Pillow>=9.4.0

# ── Agent / 网络请求 ──
requests>=2.28.0

# ── Excel 读写（流程导入导出 / 参数扫描报告） ──
openpyxl>=3.1.0

# ── 可选：模板文字识别（build_image_template_library 用到） ──
# pytesseract>=0.3.10

# ── UI 界面美化（可选推荐，缺失时自动降级到纯标准库现代扁平主题） ──
ttkbootstrap>=1.10.1

# 说明：
# 1) pywinauto_recorder.player 不在 PyPI，需从本地 Pywinauto Recorder 安装目录获取
#    （见 _launch_pywinauto_recorder.cmd）。未安装时不影响 GUI / 半自动采集 / 控件库构建。
# 2) 原生 Win32 输入钩子（win32_input_hook.py）仅使用标准库 ctypes，无需额外安装。
# 3) GUI 与 Agent 后端具备双轨自适应能力：未安装 ttkbootstrap 时自动降级为标准库现代样式，零外部依赖也能稳定运行。
