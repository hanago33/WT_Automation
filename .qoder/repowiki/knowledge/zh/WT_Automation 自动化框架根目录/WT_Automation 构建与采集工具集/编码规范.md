- 所有顶层构建脚本均以 `# encoding: utf-8` 开头并使用 argparse/tkinter 提供命令行+GUI 双入口。
- OCR 能力采用"pytesseract 优先、本地 tesseract.exe 回退"的双通道发现模式，路径搜索从 `tools/ORC` 开始。
- 输出产物统一写入 `tools/<subdir>/<category>/` 并按 JSON 索引文件（如 `templates_index.json`）维护元数据。