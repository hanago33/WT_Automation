# [OPEN] relative-region-offset

## 背景
- 症状：用户反馈 `step_16` 到 `step_26` 这段中，所有依赖父窗口相对区域的点击步骤都出现了整体偏移。
- 期望：相对区域点击应仍然命中原先记录的业务控件区域，不应因为最近对 `step_16/37/38` 的修补导致整段点击位置漂移。
- 范围：优先排查“气象数据录入”包中 `step_16` 到 `step_26` 的相对区域定位链路。

## 初始假设
1. `step_16` 对无标题主窗口的放宽，改变了相对区域动作选择的父窗口，导致后续步骤都基于错误窗口矩形求点。
2. 最近补丁误改了某个前置步骤的 `controls` / `windowTitle` / `parentWindow`，让运行时窗口搜索落到了宿主窗口或主窗体而不是目标业务弹窗。
3. `wt_flow_locator.py` 中相对区域窗口搜索链路对“无标题 WPF 窗口”的 fallback 被放宽后，前台宿主窗口被错误选为父窗口，造成统一偏移。
4. 现有 `.dbg` / 运行日志里已经有 `step_16~26` 的前台窗口和候选窗口证据，可以直接确认偏移发生在“找窗”还是“算区域”阶段。
5. 偏移不是所有步骤都变了，而是从某一步开始父窗口切换；需要找到第一个发生漂移的步骤作为根因锚点。

## 调试计划
1. 读取最新运行报告、相关 `.dbg` / `debug-*.md` / ndjson 证据，确认漂移从哪一步开始。
2. 对照 `step_16~26` 当前配置，核查 `windowTitle`、`parentWindow`、`continueWhen` 是否与历史证据一致。
3. 如现有证据不足，只在相对区域找窗链路增加最小插桩，要求用户复现一次 `step_16~26`。
4. 基于证据做最小修复，并以 `pre-fix` / `post-fix` 对比验证。

## 当前状态
- 状态：已完成首轮修复，待用户复跑验证
- 结论：现有证据更支持“相对区域求点使用的窗口矩形来源发生漂移”，而不是 `step_16~26` 的 `relativeRegion` 参数被误改。

## 证据与结论
- `flow_definition.json` 中 `step_17/19/21/22/24/25/26` 的 `parentWindow` 仍统一指向 `导入时间序列文件 | Window | WPF`，未见批量误改。
- 历史成功日志里，`step_16` 与 `step_26` 使用的窗口矩形均为 `left=115, top=25, width=2329, height=1465`。
- 最新 `.dbg` 里，`step_16` 与 `step_26` 命中的仍是 `导入时间序列文件 | Window | WPF`，但矩形统一变成 `left=128, top=76, width=2304, height=1364`。
- 新矩形与旧成功矩形之间的差值高度吻合“去掉窗口边框/标题栏后的内容区”，说明运行时并非点到了完全错误的业务窗口，更像是同一 WPF 窗口在不同会话里暴露了不同矩形基准。

## 已做修复
- 修改 `wt_flow_locator.py` 的 `get_wrapper_rectangle()`：
  - 优先通过原生窗口句柄调用 `user32.GetWindowRect` 获取稳定的顶层窗口外框。
  - 只有在拿不到句柄时，才退回原先的 `wrapper.rectangle()`。
- 该修复的目标是统一 `step_16~26` 相对区域动作的矩形基准，避免同一窗口时而使用外框、时而使用内容区导致整段点击偏移。

## 验证结果
- `py -3.11 -m py_compile WT_Automation/wt_flow_locator.py` 通过。
- `GetDiagnostics` 未发现 `wt_flow_locator.py` 新诊断。
