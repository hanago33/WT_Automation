# Simple 模式 8 板块步骤参数清单（含判定建议）

> 生成日期：2026-08-24（含用户校正 v2 + 真实参考目录样本）
> 目的：梳理总控台 Simple 模式 8 个流程板块中，每个步骤涉及的可变参数（键入值、文件输入输出地址、候选项选择），并标注判定，供用户校正。
> - **可默认**：固定业务常量，不随项目变化。
> - **需人工确认**：项目计算参数，由人工在"项目计算参数"中填写/确认（无法从目录推断）。
> - **03-WT输入解析**：从市场项目文件夹 `03-WT输入` 自动解析。
> - **04-WT输出解析**：从 `04-WT输出` 自动解析。
>
> 数据来源：`flow_packages/flow_definition_*.json`（8 个文件，只读探查）。
> 参考目录（真实样本）：`C:\Users\14830\Desktop\202608_Test`

---

## 真实参考目录结构（解析规则依据）

```
202608_Test/
├─ 03-WT输入/
│  ├─ 01-测风塔及机位点坐标/
│  │   ├─ CFT_Project_CGCS2000 43.txt        (机位点坐标，文件名含投影缩写 CGCS2000 43)
│  │   └─ JWD_Project_CGCS2000 43.txt        (测风塔坐标)
│  ├─ 02-地形图/
│  │   └─ TEST1_Project_CGCS2000 43.tif      (地形图，文件名含投影缩写 CGCS2000 43)
│  ├─ 03-测风塔数据/
│  │   └─ C1831\                              (按测风塔编号建子目录)
│  │       ├─ 1831-20230402-20240524-tim.txt  (测风塔主数据)
│  │       ├─ 1831-20230402-20240524-TI.txt   (湍流强度)
│  │       └─ 1831-20230402-20240524-TISD.txt (扇区湍流)
│  └─ 04-功率曲线/
│      ├─ 功率曲线.wtg                        (风机功率/推力曲线)
│      └─ 功率曲线.txt
└─ 04-WT输出/
   ├─ m1\  (DEMO_S2.xlsx / DEMO_S2_IEC.xlsx)
   ├─ m4\  (空，按综合生成)
   ├─ m10\ (空，按综合生成)
   ├─ DEMO_WRA2_Frequency_Matrix.txt
   └─ DEMO_WRA2_TurbulenceIntensityTotal_Matrix.txt
```

**解析规则要点（用户校正）**：
- 项目名 = 项目文件夹名（如 `202608_Test`）。
- **投影坐标系**：从文件名缩写解析，如 `TEST1_Project_CGCS2000 43.tif` → 提取 `CGCS2000 43` → 在 MUP 设置投影时搜索该缩写并选全称（替换现有写死的 "UTM 50N"）。
- **测风塔数据**：按后缀匹配 `tim`/`TI`/`TISD`，位于 `03-测风塔数据\<编号>\`。
- **功率曲线**：`04-功率曲线\功率曲线.wtg`。
- **导出分目录**：`04-WT输出\m1 / m4 / m10`（按综合 m1/m4/m10）。

---

## 板块 1：新建地形信息数据 → `flow_definition_导入地形图.json`

runtimeConfig：
- `gmExe` = `...Meteodyn WT 3.2\MeteodynWT.exe` → **可默认**
- `sourceFilePath` = `...\2地形tif\C1831.tif` → **03-WT输入解析**（`02-地形图\*.tif`）
- `outputDir` = `...\自动测试\新建项目` → **04-WT输出解析**（新建项目目录，或 `04-WT输出` 上级）
- `projectionFilePath` = `` → 随 sourceFilePath 推断

step 键入值 / 候选项：
| step | 内容 | 类型 | 判定 |
|---|---|---|---|
| step_4 | 选分组 **私有** | 候选项 | **可默认** |
| step_5 | 键入项目名称 `测试项目数据` | 文本 | **03-WT输入解析**（=项目文件夹名 `202608_Test`） |
| step_6 | 键入 `测试` | 文本 | **03-WT输入解析**（项目简称，可规则取） |
| step_8 / step_10 | 选 新建地形 | 候选项 | **可默认** |
| step_12 / step_14 | 键入地形图名 `C1831` | 文本 | **03-WT输入解析**（取 tif 文件名主体） |
| step_13 | 选数据类型=**栅格** | 候选项 | **可默认** |
| step_16 | 选单位=**米(m)** | 候选项 | **可默认** |
| step_17 | 选坐标系（原写死 UTM）→ 改为按文件名缩写选 | 候选项 | **03-WT输入解析**（CGCS2000 43 等） |
| step_18 | 选投影带（原写死 50N）→ 由缩写推导全称 | 候选项 | **03-WT输入解析**（CGCS2000 43） |
| step_19 | 选 false_northing=**0** | 候选项 | **可默认** |
| step_20 / step_22 | 键入 UTM_X `43557986.525` | 数值 | **03-WT输入解析**（tif/坐标文件元信息） |
| step_21 / step_23 | 键入 UTM_Y `5136125.583` | 数值 | **03-WT输入解析** |
| step_28 | 键入 默认水平分辨率 `50` | 数值 | **可默认**（或随 tif 推断） |
| step_29 / step_30 | 选 NoData=**否/0** | 候选项 | **可默认** |
| step_31 | 选 垂直参考=**椭球高** | 候选项 | **可默认** |

---

## 板块 2：新建气象数据 → `flow_definition_新建气象数据.json`

runtimeConfig：`gmExe` 可默认；`sourceFilePath` 14× 占位符。

写死输入路径（改为从 `03-WT输入` 解析）：
- `03-WT输入\C1831\C1831-测风塔.txt` → 实为 `03-测风塔数据\C1831\1831-...-tim.txt` → **03-WT输入解析**
- `...C1831-TI.txt` → `...-TI.txt` → **03-WT输入解析**
- `...C1831-TISD.txt` → `...-TISD.txt` → **03-WT输入解析**

step 键入值：
| step | 内容 | 类型 | 判定 |
|---|---|---|---|
| step_3 | 选 气象对象/新建 | 候选项 | **可默认** |
| step_5 | 选分组 **私有** | 候选项 | **可默认** |
| step_6 | 键入气象名 `C1831` | 文本 | **03-WT输入解析**（=测风塔编号） |
| step_7 | 选 数据来源=**Meteodyn WT mast** | 候选项 | **可默认** |
| step_9 / step_11 / step_12 | 选 垂直外推/高度模型/导入模式 | 候选项 | **可默认** |
| step_13 | 选 测风塔文件 `*-tim.txt` | 文件 | **03-WT输入解析** |
| step_14 | 选 TI 文件 `*-TI.txt` | 文件 | **03-WT输入解析** |
| step_15 | 选 TISD 文件 `*-TISD.txt` | 文件 | **03-WT输入解析** |
| step_16+ | 高度、测风塔名称键入 | 数值/文本 | **03-WT输入解析**（带入高度） |
| step_20+ | 风向扇区、是/否、单位 | 候选项 | 多数 **可默认** |

> 匹配规则：遍历 `03-WT输入\03-测风塔数据\<编号>\`，按文件名包含 `tim`/`TI`/`TISD` 后缀区分。

---

## 板块 3：新增及配置元素 → `flow_definition_导入并配置元素.json`

runtimeConfig：
- `mastImportFilePath` = `...\03-WT输入\C1831\C1831-测风塔.txt` → **03-WT输入解析**（`01-测风塔及机位点坐标\JWD_*.txt` 或 `03-测风塔数据\...\*-tim.txt`，按元素类型）
- `turbineImportFilePath` = `...\03-WT输入\测试\功率曲线.wtg` → **03-WT输入解析**（`04-功率曲线\功率曲线.wtg`）

step 键入值：
| step | 内容 | 类型 | 判定 |
|---|---|---|---|
| step_2 | 选分组 **私有** | 候选项 | **可默认** |
| step_3 | 选 新建元素/导入测风塔 | 候选项 | **可默认** |
| step_7 | 选 导入方式=**Meteodyn WT mast** | 候选项 | **可默认** |
| step_9 | 选 测风塔文件 `${mastImportFilePath}` | 文件 | **03-WT输入解析** |
| step_13 | 选 新建元素/导入风机 | 候选项 | **可默认** |
| step_17 | 选 风机文件 `${turbineImportFilePath}` (.wtg) | 文件 | **03-WT输入解析** |
| step_19 | 选 新建元素/导入机位点 | 候选项 | **可默认** |
| step_22 | 选 机位点文件 `01-测风塔及机位点坐标\CFT_*.txt` | 文件 | **03-WT输入解析** |
| step_24 | 选 新建元素/导入粗糙度 | 候选项 | **可默认** |
| step_27 | 选 粗糙度文件 | 文件 | **03-WT输入解析** |
| step_29 | 选 导入障碍物/尾流 | 候选项 | **可默认** |
| step_32 | 选 障碍物/尾流文件 | 文件 | **03-WT输入解析** |
| step_34 / step_37 | 选 网格文件 | 文件 | **03-WT输入解析** |

> 注：目录层级（mast 在 C1831、turbine 在 测试）仅为当前测试造成，后续统一路径；解析器用递归 glob 匹配即可。

---

## 板块 4：新建风机类型 → `flow_definition_新建风机类型.json`

runtimeConfig：无占位符（路径写死）。

写死输入：
- `...\03-WT输入\测试\功率曲线.wtg` → `04-功率曲线\功率曲线.wtg` → **03-WT输入解析**
- `...\测试\推力系数.wtg` → 同目录 → **03-WT输入解析**

step 键入值：
| step | 内容 | 类型 | 判定 |
|---|---|---|---|
| step_1 | 选分组 **私有** | 候选项 | **可默认** |
| step_2 | 选 新建风机类型 | 候选项 | **可默认** |
| step_4 | 选 导入=功率曲线/推力系数 .wtg | 候选项 | **可默认** |
| step_6 | 选 功率曲线 `功率曲线.wtg` | 文件 | **03-WT输入解析** |
| step_8 | 选 推力系数 `推力系数.wtg` | 文件 | **03-WT输入解析** |
| step_10 | 键入 风机名 `测试风机` | 文本 | **03-WT输入解析**（可从 .wtg 文件名推断） |
| step_12+ | 轮毂高度/风轮直径等 | 数值 | **可默认**（或随 .wtg 读取，待确认） |
| step_15 | 选 偏航误差=**无** | 候选项 | **可默认** |

---

## 板块 5：新建工程项目 → `flow_definition_创建一个新建模.json`

runtimeConfig：无占位符。

step 键入值 / 候选项：
| step | 内容 | 类型 | 判定 |
|---|---|---|---|
| step_2 | 选分组 **私有** | 候选项 | **可默认** |
| step_3 | 选 新建项目 | 候选项 | **可默认** |
| step_5 | 键入 项目名称 `测试项目` | 文本 | **03-WT输入解析**（=项目文件夹名） |
| step_8 | 选 计算类型 | 候选项 | **可默认** |
| step_11/12 | 选 地形/气象/元素对象（自动关联前序） | 候选项 | **可默认** |
| step_15 | 选 水平坐标系（原 UTM）→ 按文件名缩写 | 候选项 | **03-WT输入解析** |
| step_17 / step_18 | 键入 UTM_X / UTM_Y | 数值 | **03-WT输入解析** |
| step_20 | 键入 半径 `5000` | 数值 | **需人工确认**（项目计算参数） |
| step_23 | 选 投影带（原 50N）→ 由缩写推导 | 候选项 | **03-WT输入解析** |
| step_26 | 选 元素下拉 | 候选项 | **可默认** |

---

## 板块 6：发送CFD计算 → `flow_definition_发送CFD计算.json`

runtimeConfig：无占位符。

step 键入值：
| step | 内容 | 类型 | 判定 |
|---|---|---|---|
| step_2 | 选 CFD 计算/发送 | 候选项 | **可默认** |
| step_5 | 键入 网格水平分辨率 `22.5` | 数值 | **需人工确认**（项目计算参数） |
| step_8 | 键入 边界缓冲 `25` | 数值 | **需人工确认** |
| step_9 | 选 边界类型=**自动** | 候选项 | **可默认** |
| step_11 | 键入 最大网格尺寸 `4` | 数值 | **需人工确认** |
| step_13 | 键入 最小网格尺寸 `4` | 数值 | **需人工确认** |
| step_15 | 选 求解器/湍流模型 | 候选项 | **可默认** |
| step_18 | 选 发送队列/本地 | 候选项 | **可默认** |

> CFD 参数（22.5/25/4/4）+ 半径：统一纳入"项目计算参数"，人工确认。

---

## 板块 7：发送综合计算 → `flow_definition_发送综合计算.json`

runtimeConfig：1× `${runtime.sourceFilePath}`。

step 候选项 / 键入值：
| step | 内容 | 类型 | 判定 |
|---|---|---|---|
| step_3 | 选 综合计算/发送 | 候选项 | **可默认** |
| step_5 | 选 测风对象（自动关联） | 候选项 | **可默认** |
| step_10 | 选 版本下拉项 `Cp0.429` | 候选项 | **需人工确认**（无法从 03-WT输入 推断） |
| step_24 | 选 测风对象下拉 `M1` | 候选项 | **需人工确认**（无法从目录推断；按综合命名规则 m1/m4/m10 对应） |
| step_25 | 键入 50年回归风速 | 数值 | **需人工确认** |
| step_27 | 选 稳定性模型=**默认** | 候选项 | **可默认** |
| step_30 | 选 导出格式=**xlsx** | 候选项 | **可默认** |

> 注：测风对象 M1 虽与输出分目录 m1 对应，但其编号规则/命名无法从 03-WT输入 自动推断，故列为"需人工确认"。

---

## 板块 8：导出计算结果 → `flow_definition_导出综合计算结果.json`

runtimeConfig：无占位符。`sourceFilePath` 1×。

写死输出路径（改为从 `04-WT输出` 解析）：
- `04-WT输出\m1\WRA_综合计算_C1831.xlsx`
- `04-WT输出\m4\WRA_综合计算_C1831.xlsx`
- `04-WT输出\m10\WRA_综合计算_C1831.xlsx`
→ **04-WT输出解析**（按 m1/m4/m10 分目录）

step 键入值：
| step | 内容 | 类型 | 判定 |
|---|---|---|---|
| step_2 | 选 导出/综合计算结果 | 候选项 | **可默认** |
| step_4 | 选 导出矩阵 m1/m4/m10 | 候选项 | **04-WT输出解析**（按综合生成的分目录） |
| step_6/8/10 | 选 输出文件路径（写死 xlsx） | 文件 | **04-WT输出解析** |
| step_13 | 选 导出格式=**xlsx** | 候选项 | **可默认** |

> 综合命名规则（用户设想）：`综合1 Nomapping m1 6.25-220-125 Cp0.429` → 计算 m1/m4/m10 三个综合，导出分目录即 m1/m4/m10。解析器据 m1/m4/m10 建/读 `04-WT输出\<编号>\`。

---

## 汇总：参数类型分布

| 判定类别 | 涉及板块 | 典型参数 |
|---|---|---|
| 可默认 | 全部 | 分组=私有、数据类型=栅格、单位=米、各种"新建/默认/否/0"、边界类型=自动、偏航误差=无、导出格式=xlsx |
| 03-WT输入解析 | 板块1/2/3/4/5 | 地形 tif、测风塔 tim/TI/TISD、机位点/粗糙度/障碍物/网格 txt、.wtg 功率曲线、项目名、投影缩写(CGCS2000 43)、UTM 坐标 |
| 04-WT输出解析 | 板块8 | m1/m4/m10 的 WRA_*.xlsx 导出路径 |
| 需人工确认（项目计算参数） | 板块5/6/7 | 半径 5000、CFD 网格 22.5/25/4/4、Cp 版本(Cp0.429)、测风对象 M1、50年风速 |

---

## 用户校正记录（v2）

1. ✅ 项目名称 = 项目文件夹名解析。
2. ✅ 投影坐标系从文件名缩写解析（如 `CGCS2000 43`），设置投影时搜索缩写选全称（替换写死 "UTM 50N"）。
3. ✅ 投影带 50N / 半径 5000 / CFD 网格 随项目变化 → 纳入"项目计算参数"，人工确认。
4. ✅ Cp 版本、测风对象 M1、50年风速 **不能**从 03-WT输入 推断 → 需人工确认。
5. ✅ `.wtg/.txt` 路径层级不一致仅为测试造成，后续统一；解析器用递归 glob 兼容。
6. ✅ 导出 m1/m4/m10 按综合计算分目录（综合命名如 `综合1 Nomapping m1 6.25-220-125 Cp0.429`）。
7. 参考目录：`C:\Users\14830\Desktop\202608_Test`（含 01-测风塔及机位点坐标 / 02-地形图 / 03-测风塔数据 / 04-功率曲线 + 04-WT输出 m1/m4/m10）。
