- JSON 流程中的每个 step 统一暴露 id/name/stage/strategy/actionType/enabled/description/successLog/windowTitle/inspectHints/fallbacks/notes 等固定字段，便于运行时解析。
- UI 控件定位在 controls 数组中以 targetMethod（automation_id / control_type / name,control_type / regex / template）区分，并配合 auxChecks（IsEnabled/IsOffscreen 等）做前置断言。
- 复杂业务操作拆成 flowPackages，顶层 step 通过 actionType=flow_ref + packageRef 引用包，包内再展开为 topLevel=false 的原子 action 步骤。
- 录制脚本一律从 `from pywinauto_recorder.player import *` 导入，并以 `with UIPath(u"...||Window"):` 嵌套上下文组织 click/send_keys/right_click 等操作序列。