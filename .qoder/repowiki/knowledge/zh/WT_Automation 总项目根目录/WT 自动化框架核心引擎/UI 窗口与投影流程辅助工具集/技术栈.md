pywinauto (uia backend) + pyautogui + PIL.Image 做 UI 元素识别与截图；cv2.matchTemplate + numpy 做多尺度模板兜底匹配对抗 DPI/缩放漂移；ctypes 直接调用 user32.dll 进行窗口枚举与消息超时检测；node 子进程调用 UI-TARS CLI 执行 AI 介入步骤；环境变量 VOLC_API_KEY/UI_TARS_API_KEY 控制 AI 鉴权。