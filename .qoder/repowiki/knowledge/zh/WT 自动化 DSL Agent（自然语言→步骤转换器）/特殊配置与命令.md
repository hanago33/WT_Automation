通过环境变量配置 LLM：`WT_DSL_BASE_URL`（API 地址）、`WT_DSL_API_KEY`（密钥）、`WT_DSL_MODEL`（模型名，默认 gpt-4o）。也可在 `model_profiles.json` 中预置默认档案。命令行用法：`python -m WT_AUTOMATION_Agent.cli "点击风机类型按钮" --sequence` 或 `--gui` 启动 Web 界面。