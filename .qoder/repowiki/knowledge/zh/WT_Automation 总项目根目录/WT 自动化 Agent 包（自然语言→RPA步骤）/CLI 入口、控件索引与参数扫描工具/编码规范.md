- 对可选重型依赖（openpyxl、logging、traceback、各子包）采用函数体内 `import ...` 的延迟导入方式，避免在未被使用时触发 ImportError。
- 对外暴露的 API 统一使用 Python 3.10+ 联合类型注解（如 `str | None`、`list[str] | None`），并通过 `from __future__ import annotations` 保证兼容性。
- Excel/CSV 列名经 `_snake_case` 正则清洗后再作为键使用，确保中文/空格/特殊字符能稳定映射到合法的 stepParams 字段名。
- 面向用户的导出方法（如 `export_param_template`）会附带说明 Sheet 和样式（Font/PatternFill/Border），以蓝色表头 + 灰色提示行的视觉约定区分“可编辑区”和“只读说明”。