- JSON 流程定义集中存放于 `flow_packages/`，由总控台或编辑器通过注册表加载，禁止硬编码路径。
- UI 控件定位与模板图片分离到 `control_maps/` 与 `image_templates/`，通过名称/类别检索而非内联坐标。
- 动作字段遵循统一的 action schema，由 `wt_action_schema.py` 驱动编辑器字段显隐与必填校验。
- 运行结果统一输出结构化报告至 `logs/run_reports/*.json` 与 `logs/last_run_report.json`，便于回归分析。