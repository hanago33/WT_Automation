独立包可直接 `python -m WT_AUTOMATION_Agent.gui` 启动 GUI，或 `python -m WT_AUTOMATION_Agent.cli --gui` 通过 CLI 启动；也可作为 Python 模块直接 import `DslAgent`、`DslAgentConfig`、`DslContext` 使用；环境变量 `WT_DSL_BASE_URL`/`WT_DSL_API_KEY`/`WT_DSL_MODEL` 配置 LLM 连接。