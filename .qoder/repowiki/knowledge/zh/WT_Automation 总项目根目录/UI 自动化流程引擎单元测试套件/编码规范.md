- 通过 `TESTS_DIR = os.path.dirname(os.path.abspath(__file__))` 计算父目录并 `sys.path.insert(0, PROJECT_DIR)` 动态引入被测模块，避免安装依赖。
- 对 pywinauto 控件树使用内嵌 `FakeWrapper` 类 + `types.SimpleNamespace` 模拟 `element_info`/`window_text`/`class_name` 等接口，配合 `@pytest.fixture(autouse=True)` 的 `_patch_wrapper_getters` 统一 monkeypatch。
- 需要持久化状态的测试使用 `tmp_path` fixture 传入 `L.configure_self_heal(store_path=...)`，并在 `yield` 后清理全局状态（`_self_heal_overrides`、`_self_heal_session_hits`）。
- 可选外部依赖（如 openpyxl）在模块顶层 try/except 捕获，并以 `@unittest.skipIf(openpyxl is None, ...)` 装饰整个测试类实现条件跳过。
- unittest 用例中通过 `setUp`/`tearDown` 保存并恢复被替换的全局对象（如 `wt_flow_executor.pyautogui.click`），确保测试间隔离。