- 输出文件命名采用“基础名_YYYYMMDD_HHMMSS”时间戳后缀的版本化策略，当目标路径被占用抛出 PermissionError 时自动降级到版本化路径。
- 文档内容以 `(kind, text)` 元组列表集中声明，再通过统一的 `paragraph_xml` / `add_card` 等渲染函数映射到对应样式，避免在 XML/形状构造处散落文本。
- 所有对外输出的中间部件（document.xml、styles.xml、content-types.xml、_rels/.rels、core/app.xml 等）均以独立字符串常量函数返回，便于单独调试与替换。