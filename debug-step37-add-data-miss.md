# [OPEN] step37-add-data-miss

## 背景
- 症状：完整 46 步流程运行到 `step_39` 报错，但用户结合软件实际表现确认，真实首因出现在 `step_36 -> step_37 -> step_38` 链路：`step_36` 完成高度输入后，`step_37` 的“添加到数据-TI”没有真正生效，导致没有正确进入 `step_38/39` 所需状态。
- 时间：2026-07-02
- 范围：`step_36 -> step_39`，且用户预计 `step_43 -> step_44` 可能存在同类问题。

## 现有事实
- 日志表面显示 `step_36/37/38` 都成功，直到 `step_39` 报 `action type_text 未命中控件`。
- 用户观察到真实软件状态与日志不一致：`step_37` 没有真正完成“添加到数据-TI”。
- 历史上 `step_11 -> step_13` 已出现过类似模式，根因是输入后缺少显式提交，导致后续点击“表面成功、业务未生效”。

## 待证伪假设
1. `step_36` 输入高度后没有失焦提交，导致 `step_37` 点击时业务状态仍未满足。
2. `step_37` 的相对区域坐标存在偏差，日志记录了点击动作，但未命中“添加到数据-TI”真实热区。
3. `step_37` 点击后页面切换或数据刷新存在延迟，`step_38/39` 启动过早。
4. `step_43 -> step_44` 与 `step_36 -> step_39` 属于同一类“输入后提交/状态切换”问题，应一并验证。

## 调试计划
1. 先读取 `step_36/37/38/39/43/44` 的流程定义和最新运行报告。
2. 在不修改业务逻辑的前提下，给 `step_36/37/38/39/43/44` 补最小插桩，记录输入后前台窗口、点击点、窗口切换和后续页面状态。
3. 根据证据决定是否需要为 `step_36/43` 补 `postInputKeys`、为 `step_37/44` 补续跑条件，或修正相对区域。

## 当前状态
- 已完成首轮运行时取证，并实施最小修复，等待 post-fix 验证。

## 运行时证据
- 新日志 `.dbg/trae-debug-log-step37-add-data-miss.ndjson` 显示，`step_36` 输入前后前台都停留在 `导入统计数据文件`，且 `postInputKeys=""` 为空。
- 同一日志显示，`step_37` 点击前后前台仍是 `导入统计数据文件`，没有出现任何切换迹象；日志里的点击点解析正常，但页面状态没有变化。
- `step_39` 报错时前台仍是 `导入统计数据文件`，而不是 `打开` 文件对话框，说明 `step_39` 的失败只是前序状态未切换后的连带结果。
- 最新运行报告 `last_run_report.json` 与专项调试日志一致，表面失败出现在 `step_39`，真实首因发生在 `step_36 -> step_37`。

## 结论
- 假设 1 成立：`step_36` 输入高度后未显式失焦提交，导致 `step_37` 点击时业务状态尚未生效。
- 假设 2 暂不支持为首因：当前点击坐标解析和执行链路本身没有异常证据，且模式与之前 `step_11 -> step_12` 高度一致。
- 假设 3 不成立为首因：问题不是 `step_38/39` 启动过早，而是 `step_37` 后根本没进入应有状态。
- 假设 4 高度成立：`step_43 -> step_44` 与 `step_36 -> step_37` 为同构交互，应同步修复。

## 当前修复
- 已为 `flow_definition.json` 中的 `step_36` 和 `step_43` 补回 `postInputKeys: "{TAB}"`。
- 修复目标是让高度输入后显式失焦提交，再执行后续“添加到数据”点击，避免假成功。
