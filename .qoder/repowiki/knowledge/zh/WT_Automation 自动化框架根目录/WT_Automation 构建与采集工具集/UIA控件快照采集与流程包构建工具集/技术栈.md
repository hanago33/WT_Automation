pywinauto (uia/win32 后端) + tkinter 用于 UIA 控件树遍历与 GUI；OpenCV + PIL + numpy 用于截图边缘检测与模板裁剪；可选 Tesseract OCR（pytesseract 或 tesseract.exe）辅助模板命名；外部 .NET 工具 `uia_tree_dumper.exe` 通过 subprocess 调用实现 RawViewWalker 全量遍历。